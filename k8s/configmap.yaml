apiVersion: v1
kind: ConfigMap
metadata:
  name: open-skills-config
data:
  environment: "production"
  log_level: "INFO"
  log_format: "json"
  host: "0.0.0.0"
  port: "8000"
  cors_origins: '["https://yourapp.com"]'
